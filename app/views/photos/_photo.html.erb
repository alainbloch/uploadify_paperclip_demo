  <div class="photo">
    <%= link_to image_tag( photo.photo.url(:thumb) ), photo_path(photo) %><br>
    <%=h (photo.title || photo.photo_file_name) %> | <%= link_to 'Destroy', photo, :confirm => 'Are you sure?', :method => :delete %>
  </div>